server:
  port: 8083
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/short_link_db
    password:  123456
    username: root
  data:
    redis:
      host: localhost
      port: 6379

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      retries: 3
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      group-id: ${kafka.consumer.group-id}
      enable-auto-commit: false
      auto-offset-reset: earliest
      max-poll-records: 1000
      properties:
        spring.json.trusted.packages: "com.example.shortlink.model"
    listener:
      type: batch
      ack-mode: manual
mybatis:
  mapper-locations: classpath:mapper/*.xml

snowflake:
  worker-id: 3

#logging:
#  level:
#    org.springdoc: DEBUG
#    org.springframework.security: DEBUG
#    org.apache.ibatis: DEBUG
#    io.lettuce.core: debug
#    org.springframework.cache: debug
#    org.apache.shardingsphere: DEBUG  # 开启ShardingSphere核心日志
#    org.apache.shardingsphere.sql.parser: TRACE  # 可选：解析器日志（更详细）
#    org.apache.shardingsphere.routing: TRACE  # 可选：路由过程日志


